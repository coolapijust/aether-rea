{
	# Aether-Realist Certificate Manager
	# 仅负责向 Let's Encrypt 申请证书，不提供 Web 服务
}

# 仅用于证书申请的占位配置
# 实际流量由 Aether Backend (Host模式) 直接接管
{$DOMAIN} {
	# 开启 TLS 并指定证书存储位置
	tls {$TLS_CONFIG}
	
	# 对任何请求仅返回简单的 200 OK，证明 ACME 挑战通过
	respond "ACME Challenge Verification Endpoint" 200
}
