version: '3.8'

services:
  # 1. 业务网关 (Caddy v2)
  # 负责 H3 宣告、主动探测防御以及静态伪装分流
  gateway:
    image: caddy:2-alpine
    container_name: aether-gateway
    restart: always
    ports:
      - "80:80/tcp"
      - "443:443/tcp" # Caddy 仅负责 TCP (HTTPS + Decoy)
    environment:
      - DOMAIN=your-domain.com # 请在此处替换您的真实域名
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - aether-net
    depends_on:
      - decoy-site
      - aether-gateway

  # 2. 伪装站点 (VitePress + Nginx)
  # 作为默认回落，展示一个极其正经的开源技术文档站
  decoy-site:
    build:
      context: ./decoy
    container_name: decoy-site
    restart: always
    networks:
      - aether-net

  # 3. Aether 核心服务端 (Go)
  # 负责真正的 WebTransport 业务处理
  aether-gateway:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: aether-gateway-backend
    restart: always
    ports:
      - "443:8080/udp" # Aether 直接接管 UDP 443 (HTTP/3 WebTransport)
    environment:
      - PSK=your_super_secret_token
      - LISTEN_ADDR=:8080
    networks:
      - aether-net
    # 核心设计：Caddy (TCP/443) + Aether (UDP/443) 完美共存且互不干扰

networks:
  aether-net:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
